# Tasks 006: Bundled Skills Management

- Add global config schema, defaults, readers, writers, and tests for `skills.install.symbolic`.
- Add bundled skill install/update/remove/status primitives and tests under `packages/skills/`.
- Add a guided multi-select UI component for interactive multi-choice workflows.
- Add `mimirmesh skills install|update|remove` commands and workflow definitions.
- Update help text and command-first workflow references.
- Update README, CLI docs, runbook guidance, and ADRs.
- Remove the obsolete `MCP_LIVE_REPORT.md` artifact.
- Validate with typecheck, package tests, and build.
